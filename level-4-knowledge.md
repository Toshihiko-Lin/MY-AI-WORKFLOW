← [Level 3：跨会话记忆](./level-3-memory.md)

# Level 4：共享工作区

## AI 还是不知道你在做什么

Level 2 让 AI 认识你、遵守规矩。Level 3 让对话能接续。但 AI 还是不知道：你手上有哪些活？各项目什么状态？文件在哪里？每次都要重新交代，很烦。

**解决方案**：让 AI 和你共用一个结构化的工作区。你正常记录的东西——任务状态、项目索引、修改记录、文献笔记——AI 按需读取，不用你搬运。

---

## AI 坐进你的办公室，桌上摆着什么？

把 AI 想象成一个新来的助手。他第一天上班，需要两样东西：

1. **简报**：你手上有什么活、各自什么进度、哪个最紧急
2. **档案柜**：每个项目的详细资料在哪，需要时自己去翻

对应到文件：

| 需要 | 文件 | 什么时候看 |
|------|------|-----------|
| 简报 | 任务追踪.md | 每天开工、问进度时 |
| 档案柜 | 各项目文件夹里的索引和文档 | 进入具体任务时 |

---

## 简报：任务追踪

一张表，列出所有活跃项目的状态。AI 启动时读这张表，就知道你在做什么、什么最紧急。

```markdown
| 项目 | 阶段 | 截止 | 当前重点 |
|------|------|------|---------|
| 毕业论文 | 投稿前 | 04-26 | 第四章审校 → 全文润色 |
| 副业·给猫做衣服 | 做好了 | — | 已投喂，等待反馈 |
| 甲方项目 | 合同 | 04-25 | 设备材料整合进合同 |
```

可以在规则文件里添加时效感知规则，让AI 读到截止日期后自动标注紧急程度：

```markdown
**时效感知**：每次读取 [[任务追踪]] 时，自动对比当前日期与截止日期，
在回复中标注：🔴已逾期 / 🟡48h内到期 / 🟢从容。
```

这就是 AI 秘书每天早上给你看的简报。建一张表、配一条规则，立刻有用。

→ [templates/_AI/主页模块/任务追踪.md](./templates/_AI/主页模块/任务追踪.md)

---

## 档案柜：项目知识库

简报告诉 AI "你在做什么"，档案柜告诉 AI "每个项目长什么样"。

你的 Obsidian Vault 本身就是结构化的档案柜。不需要专门为 AI 建一套系统——你正常记录的东西，AI 按索引找过去读就行。

### 项目文件夹自带上下文

以论文项目为例：

```
研究生论文/论文/01-摸鱼场布局优化/
├── 00-索引.md          ← 入口：状态 + 导航
├── 论文信息.md          ← 元数据 + 研究内容 + 写作方向（锚定决策）
├── 修改记录/            ← 决策日志 + 活跃待办
│   ├── 20260416_仿真验证重建.md
│   └── 20260417_引用规范化.md
└── main.md             ← 论文正文（Git 管理）
```

每个文件夹自带上下文。AI 进入论文任务时，读 `00-索引.md` 就知道当前状态；需要深入时读 `论文信息.md` 获取写作方向；查待办时 `grep` 修改记录里的 checkbox。不需要一个中央文件汇总所有项目的所有信息。

### 项目知识 vs 项目记忆

文件夹里承载两种信息，区分清楚有助于维护：

**项目知识**（长期稳定）：研究方法、写作方向、期刊要求、文献笔记。变化慢，是项目的"身份"。存在 `论文信息.md`、文献笔记等固定文件里。

**项目记忆**（短期演化）：改到哪了、哪些待办没完成、上次讨论的结论。变化快，是项目的"进度"。存在 `修改记录/`、`00-索引.md` 的状态字段里。

两者都在项目文件夹内，AI 按索引找过去读就行。

---

## 导航系统：AI 怎么找到对应文件

Vault 里文件很多，AI 不能漫无目的地搜索。需要两层导航：

### 第一层：资源目录（在规则文件里）

资源目录回答的是：**`_AI/` 文件夹里有哪些 AI 专用文件，分别在什么情况下加载。**

这些文件是你专门为 AI 准备的——协作规则、任务简报、会话记忆。资源目录就是它们的目录，告诉 AI "有这些工具，遇到什么情况去拿哪个"。

在 `基础.md` 里列出：

```markdown
## 资源目录

| 资源 | 路径 | 何时加载 |
|------|------|---------|
| 关键路径 | _AI/关键路径.md | 遇到路径/设备/仓库问题 |
| 论文协作规则 | _AI/论文协作规则.md | 论文写作/修改 |
| 文献协作规则 | _AI/文献协作规则.md | 读文献/写笔记 |
| 任务追踪 | _AI/主页模块/任务追踪.md | 询问待办/项目状态 |
| 当前会话记忆 | _AI/_记忆/当前会话记忆.md | "来了/换设备" |
```

AI 根据用户意图匹配触发条件，主动加载对应资源，不等你提醒。

### 第二层：路径索引（关键路径文件）

路径索引回答的是：**Vault 里的项目文件实际在哪个路径。**

这解决的是另一个问题：Obsidian 用 Wikilink 引用文件，但 AI 需要知道真实的文件系统路径才能读取。路径索引就是这张对照表，和资源目录是两件事——资源目录管的是 `_AI/` 里的 AI 专用文件，路径索引管的是整个 Vault 里的项目文件。

```markdown
## Wikilink 对照

| Wikilink | 路径 |
|----------|------|
| [[论文01索引]] | 研究生论文/论文/01-摸鱼场布局优化/00-索引.md |
| [[论文03索引]] | 研究生论文/论文/03-可穿戴摸鱼工具设计/00-索引.md |
| [[甲方项目]] | 研究生论文/项目/04_.../甲方项目.md |
```

有了这两层，AI 的读取路径是：用户意图 → 资源目录匹配 → 路径索引定位 → 读取最小范围的文件。

---

## 按需读取，不是全量加载

AI 的上下文窗口是有限的。把所有文件一次性塞进去，既浪费 token，又降低 AI 对关键信息的注意力。

正确的做法是**分层读取**：

```
进入论文任务时：
1. 读 00-索引.md          ← 状态 + 导航（必读，很短）
2. 按需读 论文信息.md      ← 写作/修改时才需要
3. 按需 grep 修改记录/     ← 查活跃待办时才需要
4. 按需读 main.md 的特定段落 ← 改哪段读哪段
```

在规则文件里写清楚这个读取顺序，AI 就会遵守，不会一上来把整个项目文件夹都读一遍。

---

## 完整体系示意

```
_AI/
├── 基础.md                    ← 规则入口 + 资源目录（Level 2）
├── 关键路径.md                ← 路径索引 + Wikilink 对照
├── 主页模块/
│   └── 任务追踪.md            ← 全局简报（本层级）
├── 论文协作规则.md            ← 专项工作流（Level 5）
├── 文献协作规则.md            ← 专项工作流（Level 5）
└── _记忆/
    └── 当前会话记忆.md        ← 跨设备接续（Level 3）

嘉里顿大学-摸鱼工程/
├── 论文/
│   ├── 01-摸鱼场布局优化/          ← 项目知识 + 项目记忆
│   ├── 03-可穿戴摸鱼工具设计/
│   └── 学位论文/
├── 文献/                        ← 文献笔记库
├── 项目/                        ← 项目文档
└── ...
```

**你记录了什么，AI 就能读什么。** AI 不需要一个统一的"记忆文件"来了解一切——它按索引导航到对应位置，读取那里的文件，获得完整上下文。

---

## 使用 templates 快速搭建

`templates/` 文件夹提供了上述所有文件的可用模板，按归属分为两类：

**`templates/_AI/`** → 整个文件夹复制到你的 Vault 根目录，变成 `_AI/`

| 文件 | 用途 | 必填 |
|------|------|------|
| `基础.md` | 规则入口，填写用户信息和资源目录 | ✅ |
| `关键路径.md` | 填写设备路径和 Wikilink 对照 | ✅ |
| `主页模块/任务追踪.md` | 全局任务简报 | ✅ |
| `_记忆/当前会话记忆.md` | 跨设备接续（Level 3 用） | 按需 |
| `论文协作规则.md` | 论文写作专项工作流 | 按需 |
| `文献协作规则.md` | 文献阅读专项工作流 | 按需 |

**`templates/项目/`** → 每新建一个项目文件夹时，把这里的文件复制进去

| 文件 | 用途 |
|------|------|
| `00-索引.md` | 项目入口，填写当前状态和导航 |
| `论文信息.md` | 填写元数据、研究内容、写作方向 |
| `修改记录/` | 空目录，按 `YYYYMMDD_事项.md` 命名新建 |

**操作步骤：**

```
1. 复制 templates/_AI/ → 粘贴到 Vault 根目录
2. 打开 _AI/基础.md，按 [方括号] 提示填写用户信息
3. 打开 _AI/关键路径.md，填写当前设备的 Vault 路径
4. 打开 _AI/主页模块/任务追踪.md，填入当前项目和截止日期
5. 新建项目时：复制 templates/项目/ → 粘贴到项目文件夹，填写 00-索引.md 和论文信息.md
6. 在 _AI/关键路径.md 的 Wikilink 对照表里加一行新项目的路径
```

填完后删除所有 `<!-- HTML 注释 -->`，文件就可以直接给 AI 读了。

---

## 常见问题

**Q：我需要专门为 AI 整理知识库吗？**
不需要。你正常做笔记、写修改记录、维护索引文件，这些就是知识库。唯一需要额外做的是在 `基础.md` 里维护资源目录和触发条件，让 AI 知道去哪找。

**Q：文件越来越多，AI 会不会迷路？**
不会，只要导航索引维护好。AI 不扫描整个 Vault，它沿着 资源目录 → 路径索引 → 项目索引 的链条定位，每一步都是精确跳转。新增项目时在路径索引里加一行就行。

**Q：任务追踪和 Level 3 记忆文件有什么区别？**
任务追踪是你维护的全局状态表，回答"各项目当前什么情况"。记忆文件是 AI 写的对话接续摘要，回答"上次聊了什么"。两者独立维护。

---

→ 下一步：[Level 5：专项工作流](./level-5-workflow.md)
