← [Level 4：共享工作区](./level-4-knowledge.md)

# Level 5：专项工作流

## 架构搭好了，现在往里填内容

Level 4 建好了架构：资源目录告诉 AI 去哪找文件，路径索引告诉 AI 文件在哪。但目录里放什么，Level 4 没有回答。

Level 5 回答这个问题：**针对你的高频任务，往资源目录里填入专属规则文件**，让 AI 在特定场景下有稳定、可预期的行为——不用每次都重新交代背景。

---

## 示例一：论文写作工作流

### 痛点

论文写作是长周期任务，涉及多个文件（草稿、修改记录、投稿信息……），每次和 AI 沟通都要重新交代：改哪个版本、哪些内容不能动、引用格式是什么、改完存到哪。

### 解法

新建 `_AI/论文协作规则.md`，把所有约定写进去：

```markdown
# 论文协作规则

## 总则
- MD 优先：在 main.md 里写、改、存档
- 不在 TeX / doc 里撰写或修改正文
- 格式转换仅限投稿格式化，转完不回改

## 写改流程
1. 对话里打磨，说"可以写进去了"再动文件
2. Edit 工具修改 main.md 目标段落；润色时附中文草稿
3. 定位：grep -n 找行号 → 按范围读取 → 写回
4. 引用格式：[@citekey]（Pandoc Markdown）
5. 已完成的修改 → 在修改记录里打勾补结论

## 文件结构
论文/{序号}-{名称}/
├── 00-索引.md       ← 唯一入口：状态 + 导航
├── 论文信息.md       ← 元数据 + 写作方向
└── 修改记录/         ← 决策日志 + 活跃待办

## 修改记录规则
修改记录只写"为什么这么改"——决策理由、待办、结论。
- 当次改完 → 写决策摘要
- 改不完 → 用 checkbox 附决策理由，改完打勾
- 查待办：grep -rn "\- \[ \]" 修改记录/

## 告一段落
1. 修改记录按规则写入
2. 更新 00-索引.md 当前状态
3. 写作方向有变 → 更新论文信息.md
4. 更新任务追踪
```

然后在 `基础.md` 的资源目录加一行：

```markdown
| 论文协作规则 | _AI/论文协作规则.md | 论文写作/修改/投稿时加载 |
```

此后 AI 处理论文相关任务时会自动遵守这些约定，不需要每次提醒。

---

## 示例二：文献管理工作流

### 痛点

读文献、写读书笔记是反复执行的动作，但每次格式不统一，后期检索很麻烦。而且 AI 帮你总结文献时，如果不知道你的研究方向，它不知道该强调哪些内容，也不知道笔记该存到哪。

### 解法

新建 `_AI/文献协作规则.md`：

```markdown
# 文献协作规则

## 阅读流程
1. 挑文献：列目标题集后 → 补关键词搜索 → 推荐 3-5 篇
   （注明研究位置、精读部分、关联摘要，一句聚焦问题）
2. 建框架：填 frontmatter → 获取标注 → 拉全文
3. 阅读中：用户粘贴段落 → 解释/提炼 → 补入「思考与启发」节
4. 填笔记：写入 Obsidian + 同步到 Zotero；补全关联文献双链

## frontmatter
title: ""
title_zh: ""      # 英文文献填中译名
authors: ""
year:
journal: ""
doi: ""
citations:
tags: [文献]
status: 未读

## 笔记规则
- 位置：文献/{标题}.md
- 命名：英文文献用 title_zh，中文文献用原题
- 阅读结束后在文献索引追加一行：| 标题 | 摘要 | ✅已读 | 关联论文 |

## 告一段落
1. 确认笔记已写入 Obsidian
2. 在文献索引追加记录行
3. 补全双链（关联文献、关联研究方向）
4. 更新任务追踪
```

然后在 `基础.md` 的资源目录加一行：

```markdown
| 文献协作规则 | _AI/文献协作规则.md | 读文献/写笔记时加载 |
```

---

## Workflow 和 Skill 的关系

两者的核心区别在于**时间跨度**：

- **Skill** 是一次性操作——你输入 `/literature-review`，AI 执行一套步骤，返回结果，结束。它不关心你上次做了什么，也不管结果存到哪。
- **Workflow** 是长周期协作——它管的是"这件事怎么推进、结果存哪、格式是什么、哪些地方不能动"。有状态，有阶段，有你个人文件结构的深度绑定。

两者是分工，不是替代：

**Workflow 搭大框架**：你的文件在哪、笔记存哪、格式是什么、哪些地方不能动——这些约定写在 workflow 文件里。随时可以改，改了立刻生效。

**Skill 执行细则**：具体的操作步骤（搜文献、生成综述、格式化引用）封装成 skill，需要时调用。

两者叠加的效果：skill 帮你完成通用步骤，workflow 文件确保结果落到你自己的文件结构里、符合你的格式约定。缺了 workflow，每次用 skill 还是要额外交代一遍"存到哪、按什么格式"。

> **注意**：workflow 文件不会被 AI 自动感知——它需要在 `基础.md` 的资源目录里注册触发条件，AI 才知道什么时候去读它。

---

## 设计自己的工作流

**先问自己三个问题：**

1. 这个任务会反复出现吗？（一次性的不值得）
2. 每次执行时，我都要重新交代同样的背景吗？（如果是，说明有隐性约定需要显式化）
3. 结果需要落到特定的文件结构里吗？（如果是，workflow 比 skill 更合适）

三个都是"是"，就值得建一个 workflow 文件。

**最小可行版本：**

不需要一开始就写完整。三个字段就够启动：

```markdown
# [任务名称]协作规则

## 文件位置
结果存到：[路径]

## 格式约定
[文件命名规则 / 必填字段 / 模板]

## 告一段落
1. [收尾动作一]
2. [收尾动作二]
```

用几次之后，哪里 AI 没执行好，就在对应位置补一条规则。规则文件是迭代出来的，不是一次写完的。

**步骤：**

1. 把你希望 AI 遵守的约定写成规则文件，放在 `_AI/` 中
2. 在 `基础.md` 的资源目录里注册，写清楚触发条件
3. 用几周后回来修——哪里 AI 没执行好，就写得更具体

**常见适合做成工作流的任务：**

- 数据分析报告（固定模板、固定图表格式）
- 组会 PPT 整理（每次会议记录 → 结构化摘要）
- 实验记录（固定字段、固定存放位置）
- 邮件起草（给导师、给合作者的不同风格）

---

## Level 4 → Level 5：新增了什么

Level 4 建好了架构框架，Level 5 往里填入专项规则文件：

```
_AI/
├── 基础.md                    ← 规则入口 + 资源目录（Level 2）
├── 关键路径.md                ← 路径索引（Level 4）
├── 主页模块/
│   └── 任务追踪.md            ← 全局简报（Level 4）
├── 论文协作规则.md            ← ✨ Level 5 新增
├── 文献协作规则.md            ← ✨ Level 5 新增
├── [你的任务].md              ← ✨ Level 5 新增
└── _记忆/
    └── 当前会话记忆.md        ← 跨设备接续（Level 3）
```

每个节点都是独立的文件，可以单独维护和迭代，不会因为修改一处而影响其他部分。

---

用起来，然后迭代。

---

→ 下一步：[Level 6：Markdown 写作](./level-6-markdown.md)——当工作流涉及反复修改同一批文件时，先用 Markdown 替代 Word，再用 Git 接管版本历史
