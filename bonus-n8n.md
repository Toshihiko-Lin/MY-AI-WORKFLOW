← [返回 README](./README.md)

# Bonus：让 AI 主动找你

前面所有层级都是你去找 AI——打开 IDE，发起对话，它才开始工作。

这个 bonus 反过来：**让 AI 定时主动找你**——读你的邮件、看你的任务文件、在合适的时机推送消息给你。

需要一个额外的工具：n8n。

---

## n8n 是什么

n8n 是一个自动化工作流工具。你用拖拽的方式把不同的服务连起来，设定触发条件，它就在后台自动跑。

不用写代码。界面长这样：

```
[定时触发：每天早上9点]
        ↓
[读取邮件：过去24小时的未读邮件]
        ↓
[Claude：判断哪些重要，生成摘要]
        ↓
[推送通知：发到你的电脑或微信]
```

每个方块是一个"节点"，节点之间连线，数据从左往右流。

---

## 前提条件：需要一台常开机的设备

n8n 是一个需要持续运行的服务。它不能装在你的笔记本上——笔记本合盖就停了。

**有常开机设备**（台式机、NAS、实验室服务器）：在上面本地部署 n8n，数据完全在自己手里，免费无限制。推荐这条路。

**没有常开机设备**：用 [n8n.cloud](https://n8n.cloud)，官方托管版，免费套餐有执行次数限制（够个人日常用），不需要自己维护服务器。

---

## 安装

### 本地部署（有常开机设备）

需要先安装 Docker。安装完后一行命令启动 n8n：

```bash
docker run -d --name n8n -p 5678:5678 -v n8n_data:/home/node/.n8n n8nio/n8n
```

启动后浏览器打开 `http://localhost:5678`，注册账号，完成。

### 云端（n8n.cloud）

直接去 [n8n.cloud](https://n8n.cloud) 注册，免安装，登录即用。

---

## 能做什么

几个典型场景，都可以用 n8n 实现：

**定时任务摘要**：每天早上读取你的任务文件（Obsidian 或本地 `.md`），让 Claude 生成今日重点，推送到桌面或微信。

**邮件筛选提醒**：定时连接你的邮箱（IMAP），把未读邮件标题和发件人发给 Claude，让它判断哪些需要今天处理，只推送重要的，其余忽略。你不需要改变收邮件的习惯，n8n 在后台帮你过滤。

**deadline 感知**：读取任务文件里的截止日期，只在有 deadline 临近时才发通知，没有任务不打扰。

**会议前提醒**：读取日历，会议开始前30分钟推送议程摘要。

---

## 推送渠道

**桌面通知**：Windows 上可以用 [ntfy](https://ntfy.sh) 或 [Bark](https://bark.day.app)（iOS），n8n 里用 HTTP Request 节点调用一个 API，消息直接弹到桌面或手机。

**Server酱**：国内专门做微信推送的服务，[sct.ftqq.com](https://sct.ftqq.com) 注册后拿到一个 API key，n8n 里一个 HTTP Request 节点，消息发到你关注的公众号。免费额度够个人日常用。

**邮件**：n8n 原生支持 SMTP，发到你的工作邮箱，适合不需要即时提醒的场景。

---

## 第一个流程：重要邮件提醒

以"每天早上检查邮件，推送重要邮件摘要"为例，流程结构：

```
Cron（每天 8:30）
    ↓
Email Read（IMAP 读取过去24小时未读邮件）
    ↓
Claude（判断重要性，生成摘要）
    ↓
HTTP Request（Server酱 / ntfy 推送）
```

在 n8n 里，每个节点都有图形化的配置界面，填入邮箱地址、密码、API key，连线，保存，激活。不需要写任何代码。

Claude 的 prompt 大概是：

```
以下是今天的未读邮件列表（发件人 + 主题）：
{{邮件列表}}

请判断哪些需要今天处理，哪些可以忽略。
输出格式：需要处理的邮件列表 + 一句话说明原因。
如果没有需要处理的邮件，回复"今天没有紧急邮件"。
```

---

## 进阶方向

n8n 的节点库覆盖了大多数常用服务（Google Calendar、Notion、GitHub、微信企业号……），基本思路都是一样的：触发 → 读数据 → Claude 处理 → 推送结果。

搭好第一个流程之后，后面的流程照葫芦画瓢。

更复杂的场景——比如 AI 读完邮件后自动起草回复草稿、或者根据任务优先级重新排序你的待办——也都可以实现，但那已经是另一个话题了。

---

> 这个 bonus 只是入门介绍。n8n 本身有完整的官方文档和社区模板，搭好环境后直接去 [n8n.io/workflows](https://n8n.io/workflows) 找现成的流程改改就能用。
