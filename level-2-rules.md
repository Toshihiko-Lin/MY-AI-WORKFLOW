← [Level 1：工具入门](./level-1-setup.md)

# Level 2：定制角色与规则

## AI 每次都不认识你，这个问题可以解决

每次打开新的 AI 对话，它都是一张白纸——不知道你是谁、你在做什么、你有什么习惯。你每次都要重新交代背景，而且每次交代的深度不一样，AI 的表现就忽好忽差。

**解决方案**：写一个规则文件，AI 启动时自动读取，从此「认识」你。

这个文件叫 `CLAUDE.md`（Claude Code）或 `AGENTS.md`（Codex CLI），放在指定位置后每次启动自动加载。如果你在用 Obsidian，建议把实际内容放在 Vault 里统一管理，全局配置文件只写一行"去那里读"——这样规则跟着 Obsidian 同步，多设备也不用重复维护。

---

## 规则文件写什么？

这里写的是**始终生效**的规则——不管你在做什么任务，每次启动 AI 都自动生效。

> 任务类型规则（写论文时走什么流程、读文献时怎么做笔记）是另一回事，在 Level 5 里讲。

规则文件没有固定格式，写什么、怎么写完全由你决定。几个真实例子：

```markdown
先讨论再动文件    避免改了方向错的东西
只改要求的范围    不顺带重构、不扩充周边代码
按需加载资源      不一次性读取所有文件浪费上下文
启动先汇报状态    说"来了"时先读任务追踪，再等指令
```

常见的几个维度：

- **用户信息**：你是谁、研究方向、交流偏好——AI 据此调整回答风格和深度
- **行为约束**：什么情况下才能动文件、Git 操作怎么处理、格式有什么要求——约定边界，避免 AI 自作主张
- **资源目录**：你的知识库里有哪些文件、各自什么用、什么时候加载——AI 按需读取，不会一次性把所有文件都塞进上下文
- **启动握手**：你说"来了""继续"这类模糊信号时，AI 该做什么——先汇报状态还是等你发指令
- **结束流程**：收工时 AI 该整理什么、写到哪里——让每次结束都有明确交接

资源目录里可以用 `[[文件名]]` 引用 Vault 里的其他文件，AI 会把它解析成实际路径去读取。这是 Level 3 记忆系统的基础语法，后面会用到。

参考模板：→ [templates/_AI/基础.md](./templates/_AI/基础.md)

---

## 规则文件放在哪？

### 先建一个 `_AI/` 文件夹

建议在 Obsidian Vault 根目录新建一个 `_AI/` 文件夹，专门存放所有 AI 相关文件：规则文件、记忆文件、专项工作流。后续 Level 3 和 Level 4 的文件都会放在这里。

```
Vault根目录/
└── _AI/
    ├── 基础.md              ← 规则文件（本层级）
    ├── _记忆/               ← 记忆文件（Level 3）
    └── [专项工作流].md      ← 任务规则（Level 5）
```

全局规则文件（`CLAUDE.md` / `AGENTS.md`）指向这里的 `基础.md`，AI 启动时自动读取。

### 什么是"项目"

CLI 工具启动时，会把你当前所在的文件夹视为「项目」。你的论文草稿文件夹、代码仓库、Obsidian Vault——只要你在那个目录下启动 AI，它就是当前项目。

规则文件有两个层级：

| 层级 | 路径 | 生效范围 |
|------|------|---------|
| 全局规则 | `~/.claude/CLAUDE.md`（Claude）/ `~/.codex/AGENTS.md`（Codex） | 所有项目，每次启动都加载 |
| 项目规则 | 项目目录下的 `CLAUDE.md` / `AGENTS.md` | 仅当前项目，进入该目录才加载 |

两者同时存在时叠加生效，项目规则优先级更高。

**注意**：层级越多，AI 需要处理的规则越多，执行稳定性越低。全局规则是最可靠的——它最先加载，AI 注意力最集中。项目规则叠在上面，内容越多越容易被忽略或执行不一致。建议把核心约定放在全局规则里，项目规则只写该项目特有的少量补充。

> 工具差异：Claude Code 用 `CLAUDE.md`，Codex CLI 用 `AGENTS.md`，内容格式完全相同，只是文件名不同。

### 怎么选？

**情况一：单 AI，单设备**

直接把规则写在 `CLAUDE.md` 里，最简单。

```markdown
## 用户
...

## 规则
...
```

**情况二：多 AI，单设备**

Claude Code 和 Codex CLI 的全局规则文件名不同，但可以都指向 Obsidian 里同一份 `基础.md`。规则只维护一份，两个 AI 共享。

`~/.claude/CLAUDE.md` 和 `~/.codex/AGENTS.md` 写同样的内容：

```markdown
# 启动规则
读取 [Vault路径]/_AI/基础.md，按其中内容行事。
```

**情况三：多设备（单/多 AI）**

每台设备的 Vault 路径不同，在全局规则里列出所有设备的路径。AI 按顺序尝试，找到第一个存在的文件即用。Obsidian 负责跨设备同步，规则零维护。

```markdown
# 启动规则
按顺序尝试读取，第一个存在的文件即为规则来源：
1. [设备A的Vault路径]/_AI/基础.md
2. [设备B的Vault路径]/_AI/基础.md
```

---

## 写规则的原则

| 原则 | 错误示范 | 正确示范 |
|------|---------|---------|
| 具体 > 模糊 | "小心操作文件" | "修改文件前展示 diff，确认后再写入" |
| 触发条件清晰 | "需要时加载任务追踪" | "用户询问待办/项目状态时加载" |
| 从小开始 | 一次写 50 条规则 | 先写 10 条，用几周后再扩充 |
| 用了再迭代 | 写完就放着 | AI 做了让你不满意的事，加进规则 |

---

## 参考模板

→ [templates/_AI/基础.md](./templates/_AI/基础.md)（复制后按注释填写即可）

---

→ 下一步：[Level 3：跨会话记忆](./level-3-memory.md)

→ [Level 4：共享工作区](./level-4-knowledge.md)——AI 按需读取任务简报和项目上下文，不从零开始理解你的项目

→ [Level 5：专项工作流](./level-5-workflow.md)——按任务类型激活的流程规则（写论文、读文献、做实验记录……），类似 skill，触发时才生效
