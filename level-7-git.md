← [Level 6：用 Markdown 写作](./level-6-markdown.md)

# Level 7：版本管理

## 你的论文文件夹现在长什么样？

```
论文终稿.docx
论文终稿_改.docx
论文终稿_导师批注版.docx
论文终稿_打死都不改版.docx
论文终稿_打死都不改版2.docx
```

这是大多数人的现状，包括我之前也这样。版本靠文件名区分，改动历史靠记忆，换台电脑就一团乱。

用 Git 之后是这样的：

```
main.md     ← 只有一个文件，永远是最新版
            ← 每一稿都在版本记录里，随时回溯
            ← 每次存档附一句说明："补充实验二数据"
```

Git 是一个本地版本控制工具，做一件事：**记录文件在每个时间点的状态，让你随时能回到任意历史版本**。它和 GitHub 是两件事——GitHub 是把记录上传到云端的网站，本章先用本地 Git，不需要注册账号，不需要联网。

---

## 什么时候需要 Git

不是所有文件都需要 Git，不用有压力。

**需要**：你会反复修改同一批文件，而且需要能回到任意历史版本。典型场景：论文正文、代码、长期维护的文档。

**不需要**：文献笔记（写完基本不改）、会议记录（追加不修改）、Obsidian Vault（用 Obsidian 自带同步就够了）。

---

## 前置条件

- 安装 [Git](https://git-scm.com/)（安装时一路默认就行）
- 使用 VS Code 或 Cursor（Level 6 里装好了）
- 论文文件是 `.md` 格式

---

## 第一次：初始化

在 VS Code / Cursor 里打开论文文件夹（文件 → 打开文件夹）。

左侧 Source Control 面板会提示"这个文件夹还没有 Git 仓库"，点 **Initialize Repository**，完成。

只需要做一次，之后就一直有了。

---

## 保存 vs 存档

这里有个容易混的概念，先说清楚：**Ctrl+S 保存文件和 Git 存档是两件不同的事**。

- **Ctrl+S（保存文件）**：把当前内容写入磁盘。保存一次就覆盖上一次，上一个版本没了。这是 Word 的逻辑。
- **Git 存档（Commit）**：在版本历史里打一个快照。文件还是那个文件，但这个时间点的状态被永久记录，以后随时能回来。

两个都要做。写的时候照常 Ctrl+S，觉得"这个版本值得留下来"时再做一次 Git 存档。

---

## 日常存档：三步搞定

左侧 Source Control 面板会列出所有**已保存但还没存档**的改动文件：

```
1. 点文件名旁边的 + 号（加入暂存区）
2. 在顶部输入框填一句说明，比如"补充第三章样本量说明"
3. 点 ✓ Commit
```

**嫌麻烦的话，直接让 AI 帮你做**——告诉它"帮我提交一下"，它会自动完成步骤 1 和 3。你只需要确认或改一下它写的说明——这是唯一需要你判断的地方：说明写得准不准，以后能不能靠这句话找到这个版本。

**存多频繁都行**：

- 改一次存一次——颗粒度最细，每步都能回退
- 一天结束时存一次——适合稳定推进的阶段
- 一个大版本完成后存一次——适合整体迭代

存档是免费的，多存没有坏处。

---

## 查看历史和回退

装一个 **Git Graph** 插件（扩展市场搜索"Git Graph"，点安装）。装好后 Source Control 面板顶部出现"View Git Graph"按钮。

点开后能看到完整的版本历史时间线，每个节点是一次存档，点进去能看到那次改了什么——哪行加了、哪行删了，一目了然：

```diff
- 本研究采用问卷调查法，共收集样本 120 份。
+ 本研究采用问卷调查法，共收集有效样本 147 份，剔除无效问卷 12 份。
```

**想回到某个历史版本**：在 Git Graph 里右键点目标节点 → **Checkout**。文件夹里的文件立刻变成那个版本的状态。

看完想回到最新版：右键最新节点 → **Checkout**。

全程不需要输入任何命令。

---

## 和 AI 工作流的配合

Git 和 Level 4 里建立的修改记录文件分工不同，不重复：

| | 记录什么 | 谁来写 |
|--|---------|--------|
| Git 提交记录 | 改了什么（diff 自动生成） | 你写一句说明 |
| 修改记录文件 | 为什么这么改、待办、决策 | AI 协助整理 |

Git 是自动的版本快照，修改记录是人工的决策日志。在论文协作规则里加一条就能让 AI 记住这个习惯：

```markdown
## 版本管理
每次告一段落后提交一次 Git，说明写本次改动的核心内容（一句话）。
```

---

## 常见问题

**Q：`.gitignore` 是什么，需要配置吗？**

`.gitignore` 告诉 Git 哪些文件不需要追踪。在 VS Code 左侧文件资源管理器里右键论文文件夹根目录 → **新建文件**，文件名输入 `.gitignore`（开头有个点，Windows 不常见但完全正常）。写入：

```
*.pdf
*.docx
*.aux
*.log
.DS_Store
```

也可以直接让 AI 帮你：告诉 Claude Code"帮我建一个 .gitignore，排除 pdf、docx、aux、log 文件"。

**Q：Git 和 Obsidian 能一起用吗？**

可以，但建议分开：Obsidian Vault 用 Obsidian 自带同步，论文工作文件夹单独用 Git。两套系统叠在一起容易乱。

---

## 进阶：多端同步

本地 Git 的记录只在一台电脑上。换台电脑继续写、或者电脑坏了，记录就没了。

解决方案是把记录推送到一台**远程服务器**，两台电脑都连着它，改动双向同步。有两种选择：

### 选项一：GitHub（公有云）

[GitHub](https://github.com) 免费账号支持私有仓库（只有你自己能看）。

在 VS Code / Cursor 里，Source Control 面板有 **Publish to GitHub** 按钮，登录账号后一键完成。之后每次提交后点 **Sync Changes** 推送到云端。

换台电脑时，在 GitHub 复制仓库地址，VS Code 里 **Clone Repository** 粘贴地址，整个文件夹连同所有历史记录都下载下来。

适合：不介意数据存在第三方服务器、需要随时随地访问。

### 选项二：私人服务器（Gitea）

如果你有一台常开机的电脑（台式机、NAS、实验室服务器），可以在上面搭一个 [Gitea](https://gitea.io)——功能和 GitHub 一样，数据完全在自己手里。

搭好后操作和 GitHub 完全相同：提交 → Sync Changes → 另一台电脑 Pull。

适合：数据不想出局域网、已经有常开机的设备。

### 日常操作

```
开工前：Pull（拉取最新版本）
收工后：Commit → Sync Changes（推送到远程）
```

**为什么开工前要先 Pull？** 如果你在另一台电脑上改过，本地就落后了。不 Pull 直接改，两台电脑的版本会分叉，需要手动合并，对新手来说很麻烦。养成"开工先 Pull"的习惯，这个问题基本不会出现。

---

→ 这是最后一层了。从 Level 2 的规则文件，到 Level 7 的版本管理，每一层都可以单独用，也可以叠起来。先用起来，遇到不顺手的地方再调整就好。
