← [返回 README](./README.md)

# Level 1：工具入门

## 你现在用 AI 的方式，和它真正能做的事，差距有点大

你现在大概是这样用的：打开网页，把内容粘贴进去，等它回复，再把结果粘贴回来。每次都要手动搬运，而且 AI 只能看到你粘贴的那一块，看不到文件的全貌。

**CLI 工具**（Claude Code / Codex CLI）不一样——它直接在你的电脑上工作：

| | 网页版 AI | CLI 工具 |
|--|---------|----------|
| 读取文件 | 看你上传的内容 | 主动读取任意文件 |
| 关联文件 | 无法获取上传文件所引用的其他文件 | 自己追踪文件间关联 |
| 修改文件 | 手动替换 | 直接写入 |
| 执行命令 | 不能 | 可以 |
| 记忆 | 能积累模糊印象（偏好、研究方向） | 能追踪具体任务状态（见 Level 3） |

网页版 AI 的记忆是"模糊的"——它知道你喜欢什么样的表达、研究方向是什么，但没办法告诉你"你上次改到第三章，第五章还没动"。CLI 工具通过读写文件记录精确的任务状态，能给你这种具体的接续建议。

---

## 你现在用的是哪种方式？

Claude Code / Codex 有三种入口，能力一样，用法略有不同：

| 方式 | 怎么用 |
|------|--------|
| **图形客户端** | Claude / ChatGPT 桌面 App，内置文件操作界面 |
| **IDE 插件** | VS Code / Cursor 扩展市场搜索安装，直接在编辑器里用 |
| **终端 CLI** | 安装后在终端输入 `claude` 或 `codex` 启动 |

**如果你已经在用图形客户端或 IDE 插件**，可以跳过下面的安装步骤，直接去[第一次使用实验](#第一次使用感受能力边界)试试，然后去 [Level 2](./level-2-rules.md)——规则文件对三种方式都通用。

**如果你还没装任何东西**，按下面的步骤来，我们从终端 CLI 开始，一步步搭起来。

---

## 安装

根据你有的订阅，按官方文档装好，装完回来继续：

- **Claude Pro / Max 订阅** → [claude.ai/code](https://claude.ai/code)，装好后在终端运行 `claude`
- **ChatGPT Plus 订阅** → [github.com/openai/codex](https://github.com/openai/codex)，装好后在终端运行 `codex`

---

## 第一次使用：感受一下它能做什么

装好之后，先试两个小实验：

**实验一：让它自己去找**
```
帮我列出这个文件夹里的所有 .md 文件
```
它会自己扫描文件夹，不需要你告诉它文件在哪。

**实验二：让它自己去读**
```
读一下 draft.md 的第一段，帮我判断逻辑是否清晰
```
它会自己打开文件读内容，你不用复制粘贴任何东西。

---

## 放心用，它不会乱动你的文件

CLI 工具在改文件之前会先给你看改了什么（diff），等你确认才会真的写入。养成看一眼 diff 再点确认的习惯就够了，不用担心它偷偷改了什么。

---

→ 下一步：[Level 2：定制角色与规则](./level-2-rules.md)
