← [Level 2：定制角色与规则](./level-2-rules.md)

# Level 3：跨会话记忆

## 关窗就忘了，下次得重新说

AI 的单次对话可以很长，上下文一直连贯。但**关掉窗口就归零**——下次打开，它不知道你上次聊了什么、改到哪了、接下来打算做什么。每次都要重新交代，很烦。

---

## 两种接续方式

### 方式一：本地记忆系统（单设备、单 AI）

Claude Code 自带 `memory/` 目录，Codex 有对话历史。单设备、单 AI 的场景下，**本地记忆足够用**，你不需要额外维护任何文件。

AI 会自动在本地积累你的偏好、项目上下文、工作习惯。下次开对话时它能"想起来"一部分。这不是完美的记忆，但对大多数单设备场景够用。

### 方式二：共享记忆文件（多设备 / 多 AI 切换）

本地记忆不跨设备、不跨工具。一旦你换了电脑或换了 AI，上次的对话上下文就断了。

| 场景 | 本地记忆够用？ | 需要共享文件？ |
|------|:---:|:---:|
| 一台电脑，一个 AI | ✅ | ❌ |
| 一台电脑，Claude + Codex 交替用 | ❌ | ✅ |
| 实验室 + 宿舍两台电脑 | ❌ | ✅ |
| 三台设备 + 两个 AI | ❌ | ✅ |

这时候需要一个**两边都能读写的共享文件**来接续对话。

---

## 共享记忆文件怎么做

### 核心思路

收工时，AI 把本次对话的接续信息写入一个文件。下次开对话时，AI 先读这个文件，就能接上。

这个文件只回答一个问题：**上次对话聊了什么、下次从哪开始**。不是任务追踪，不是项目状态表——那些是 Level 4 知识库的事。

### 文件结构

```
Vault根目录/
└── _AI/
    └── _记忆/
        └── 当前会话记忆.md    ← 唯一文件，每次覆写
```

只需要一个文件。每次覆写（不是追加），保持轻量。

### 记忆文件格式

```markdown
# 当前会话记忆

## 最近一次会话
- 日期：2026-04-25
- 设备：宿舍
- 完成：重写了第三章实验方法部分，补充了样本量说明
- 未完成：第五章讨论的局限性还没动
- 下次建议起点：从第五章局限性开始，参考修改记录里的 Section 5 待办
```

一段话，说清楚上次做了什么、没做完什么、下次从哪接。越具体越好——"从第五章局限性开始"比"继续论文"有用得多。

### 在规则文件里配置触发

在 `基础.md` 里加两条规则：

```markdown
**启动握手**：用户说"来了/回来了/继续"时，先读取 [[当前会话记忆]]，
汇报上次进度，再等用户决定。

**接续写入**：仅在用户说"收工/下了/换设备"时，才更新 [[当前会话记忆]]。
平时对话中默认不写。
```

---

## 使用流程

### 收工时

```
你：收工

AI：好的，整理本次进度……
    [写入当前会话记忆.md]
    已更新，下次说"来了"我会汇报进度。
    本次完成：重写了第三章实验方法，补充了样本量说明。
    未完成：第五章局限性还没动。
    建议下次从第五章开始。
```

### 开工时（换了设备或换了 AI）

```
你：来了

AI：[读取当前会话记忆.md]
    上次（4月25日，宿舍）：
    ✅ 重写了第三章实验方法
    ✅ 补充了样本量说明
    ⬜ 第五章局限性未完成
    建议从第五章开始，要继续吗？
```

---

## 多设备路径映射

不同设备的 Vault 路径不同。在规则文件或导航索引里维护一张映射表，AI 自动适配：

```markdown
| 设备 | Vault 路径 |
|------|-----------|
| 教研室 | C:\Users\Alan_\Documents\Obsidian Vault\MY DATABASE |
| 宿舍 | E:\Obsidian Database\My Database |
```

全局规则文件（`CLAUDE.md`）用路径回退实现自动识别：

```markdown
按顺序尝试读取，第一个存在的文件即为规则来源：
1. C:\Users\Alan_\Documents\Obsidian Vault\MY DATABASE\_AI\基础.md
2. E:\Obsidian Database\My Database\_AI\基础.md
```

---

## 常见问题

**Q：单设备需要这个文件吗？**
不需要。AI 自带的本地记忆在单设备场景下够用。只有跨设备或跨 AI 工具时才需要共享文件。

**Q：记忆文件会越来越长吗？**
不会。每次收工覆写，只保留最近一次会话的接续信息。历史记录不在这里存——在日记或项目的修改记录里。

**Q：如果忘记说"收工"就关窗口了怎么办？**
下次开工时 AI 读到的是上上次的记忆。口头补充"上次我还改了……"就行，AI 会把这信息纳入当前对话。

**Q：和任务追踪是什么关系？**
记忆文件回答"上次对话聊了什么"，是对话级别的接续。任务追踪回答"各项目当前状态如何"，是项目级别的全局视图。两者独立维护，不要混在一起。

---

## 参考模板

→ [templates/_AI/_记忆/当前会话记忆.md](./templates/_AI/_记忆/当前会话记忆.md)

---

→ 下一步：[Level 4：共享工作区](./level-4-knowledge.md)
