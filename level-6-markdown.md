← [Level 5：专项工作流](./level-5-workflow.md)

# Level 6：用 Markdown 写作

## 先说一件可能让你皱眉头的事

这一章要说服你放弃 Word，改用 Markdown 写论文。

我知道这听起来有点过分。Word 是你用了十几年的东西，界面熟悉，格式直观，导师也在用。切换有成本，有摩擦，有风险。

所以我先把理由说清楚，再讲怎么做。如果你读完觉得不值得，完全可以跳过这章——Level 2–5 的工作流对 Word 用户同样有效，只是 AI 协作的深度会打折扣。

---

## Word 的问题出在哪里

### `.docx` 不是文本文件

你以为 Word 文件里存的是你写的字。实际上，`.docx` 是一个伪装成文档的 ZIP 压缩包，里面是 XML 代码加图片资源。

用解压软件打开一个 `.docx`，你会看到这样的东西：

```xml
<w:p><w:pPr><w:pStyle w:val="1"/><w:jc w:val="center"/>
</w:pPr><w:r><w:rPr><w:b/><w:color w:val="000000"/>
</w:rPr><w:t>第三章 实验方法</w:t></w:r></w:p>
```

这是"第三章 实验方法"六个字在 Word 文件里的真实样子。

AI 工具在你的文件系统里工作，遇到 `.docx` 就像遇到这堆代码。它能"打开"，但要先穿透 XML 标签才能定位内容，改动也难以精确写回。你说"帮我改第三章的措辞"，AI 要在这堆标签里找到那六个字，改完再把格式标签原样写回去——这个过程容易出错，而且你看不到它改了什么。

**实际后果**：你会退化回手动复制粘贴，和网页版 AI 没有本质区别。

### Git 对 Word 几乎失效

Git 的核心能力是显示"改了哪几行"。这对纯文本文件完美工作：

```diff
- 本研究采用问卷调查法，共收集样本 120 份。
+ 本研究采用问卷调查法，共收集有效样本 147 份，剔除无效问卷 12 份。
```

但 `.docx` 是二进制格式。Git 只能告诉你"这个文件改了"，无法显示改了哪里。版本历史变成了一堆没有意义的快照。（Git 是下一章的内容，这里先知道 Word 和它不兼容就够了。）

### Token 消耗

AI 读 `.docx` 时，格式标签和实际内容一起计入 token。同样一篇论文，`.docx` 的消耗可能是 `.md` 的数倍。

---

## Markdown 是什么

Markdown 是一种纯文本格式。你写的是普通文字，用几个符号标记结构：

```markdown
# 第三章 实验方法

## 3.1 研究设计

本研究采用**问卷调查法**，共收集有效样本 147 份。

- 样本来源：西北工业大学在校学生
- 收集时间：2025 年 10 月
- 剔除标准：填写时间少于 3 分钟
```

渲染后就是带格式的文档，符号本身不显示。

你需要记住的全部语法：

| 效果 | 写法 |
|------|------|
| 一级标题 | `# 标题` |
| 二级标题 | `## 标题` |
| 粗体 | `**文字**` |
| 斜体 | `*文字*` |
| 列表 | `- 条目` |
| 引用 | `> 引用文字` |

十分钟够了，其他语法遇到再查。

### 视觉体验

"盯着符号写字不舒服"是真实的感受，不是误解。两点值得知道再做决定：

**视觉差距比预期小。** VS Code 内置 Markdown 预览，左边写、右边实时渲染。Obsidian 更接近所见即所得——打完 `**粗体**` 立刻显示成粗体，符号消失。上手之后和 Word 的差距往往比想象中小。

**写作场景和阅读场景不同。** Word 的视觉优势在"看最终效果"，而 AI 协作的核心场景是高频修改——AI 改了什么、改了哪里、能不能回退。这个场景下纯文本反而更清晰。

---

## 内容和格式分开

Word 把"写什么"和"长什么样"混在一起，写作时要同时处理字体、行距、标题样式——这些和内容无关的事情不断打断思路。

Markdown 的前提是：**写作阶段只关心内容，格式在最终输出时才介入**。

这不是"先将就着用 Markdown，最后再改格式"——而是**写作和排版本来就是两件事，只是 Word 把它们强行混在一起了**。

---

## 如果必须提交 Word 或 LaTeX

有些情况确实无法回避：期刊要求 `.docx`、导师批注依赖 Track Changes、投稿系统只接受 `.tex`。

遇到标准模板时，有两条路：

### 路线一：让 AI 转换（Pandoc）

适合模板格式简单的情况——只规定字体、行距、标题层级，没有复杂封面页或多栏布局。

直接告诉 Claude Code"帮我用 Pandoc 把 main.md 转成 Word，套用这个模板文件"，它会处理安装、命令、参数的全部细节。你只需要提供模板文件，检查转换结果。

### 路线二：移植内容

适合模板复杂、格式要求严格的情况（学校毕业论文模板、期刊双栏排版）。

在 Markdown 里把内容打磨好，最终提交前打开模板文件，把正文段落粘贴进去。格式由模板保证，你只负责内容。对于复杂模板，这条路反而比转换更干净。

### 判断标准

| 模板特征 | 推荐路线 |
|---------|---------|
| 只规定字体、行距、标题样式 | 让 AI 用 Pandoc 转换 |
| 有封面页、页眉页脚、特定布局 | 移植内容至模板 |
| LaTeX 模板有复杂宏包依赖 | 移植内容至模板 |
| 需要导师用 Track Changes 批注 | 移植内容或转换至 Word |

两条路的共同前提：**写作阶段在 Markdown 里完成，格式问题留到最后一步**。

---

## 用什么软件写

Markdown 是纯文本，记事本也能打开。但记事本没有预览，没有 AI，没有版本管理。你需要一个把这三件事放在同一个窗口里的工具。

推荐 VS Code 或 Cursor，二选一：

| | VS Code | Cursor |
|--|---------|--------|
| 费用 | 免费 | 免费套餐够用，Pro 版付费 |
| AI | 需要安装 Claude Code 插件 | 内置 AI，开箱即用 |
| 适合谁 | 已有 Claude 订阅 | 想要备用 AI、额度见底时有退路 |

两者界面几乎相同，本章后面的步骤对两者都适用。

安装完打开论文文件夹，你会看到：

```
┌─────────────────────────────────────────────────────┐
│  ┌──────────────┐  ┌───────────────────────────┐   │
│  │ 文件资源管理器│  │  论文.md（正在编辑）       │   │
│  ├──────────────┤  ├───────────────────────────┤   │
│  │ Git 提交面板 │  │  Claude Code 侧边栏        │   │
│  │ • 暂存改动   │  │  > 帮我改一下第三章的措辞  │   │
│  │ • 填写说明   │  │  AI: 已修改，请查看 diff  │   │
│  │ • 一键提交   │  │                           │   │
│  └──────────────┘  └───────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

AI 改完，左边面板看 diff、点提交，不需要切换任何窗口。

---

→ 下一步：[Level 7：版本管理](./level-7-git.md)——用 Git 接管论文的版本历史，随时能回到任意一稿
